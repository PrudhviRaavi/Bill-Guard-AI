[phases.setup]
nixPkgs = ["python311", "python311Packages.pip"]

[phases.install]
cmds = ["cd 'electric bill app/backend' && python -m pip install -r requirements.txt"]

[start]
cmd = "cd 'electric bill app/backend' && python -m uvicorn main:app --host 0.0.0.0 --port $PORT"
